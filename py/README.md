# jpk-reader-rs

Read JPK AFM data files using a Rust backend.

## Develop
Use [`maturin`](https://www.maturin.rs/) to build the Python package.

### Publish
Publish to [PyPI](https://pypi.org) with `maturin publish` with username `__token__` and password of the API token.